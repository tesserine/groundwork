{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/tesserine/groundwork/main/schemas/work-unit.schema.json",
  "title": "Work Unit",
  "description": "Validates the content of a work-unit artifact. A work unit decomposed from requirements, with acceptance criteria that thread through the entire execution phase.",
  "type": "object",
  "required": ["title", "description", "acceptance_criteria", "handle"],
  "additionalProperties": false,
  "properties": {
    "title": {
      "type": "string",
      "description": "What this work unit is.",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "What needs doing.",
      "minLength": 1
    },
    "acceptance_criteria": {
      "type": "array",
      "description": "Discrete, verifiable conditions for done.",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    },
    "handle": {
      "$ref": "#/$defs/handle"
    },
    "scope": {
      "type": "array",
      "description": "In-scope boundaries for this work unit.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "out_of_scope": {
      "type": "array",
      "description": "Explicit exclusions adjacent to this work unit.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "dependencies": {
      "type": "array",
      "description": "Work units that must be complete before this starts.",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"
      }
    }
  },
  "$defs": {
    "handle": {
      "type": "object",
      "required": ["id", "display"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Connector-issued identity token; compared for equality only by the engine. Must be a complete, scoped work-unit identity per FORGE-CAPABILITY.md (Forge Work-Unit Identity); the prose is authoritative.",
          "minLength": 1
        },
        "display": {
          "type": "string",
          "description": "Human-readable rendering of the handled object.",
          "minLength": 1
        }
      }
    }
  }
}

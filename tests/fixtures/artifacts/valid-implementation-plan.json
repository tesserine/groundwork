{
  "work_unit": "work-unit-492-contract-machine",
  "summary": "Add a JSON Schema validation middleware to the ingestion endpoint that checks records against the expected schema before forwarding to the pipeline",
  "design_decisions": [
    {
      "decision": "Use JSON Schema for record validation",
      "rationale": "Schemas already exist for all record types and can be reused without new dependencies"
    },
    {
      "decision": "Validate at the HTTP handler level before enqueuing",
      "rationale": "Fail fast — rejected records never touch the queue, reducing wasted processing"
    }
  ],
  "affected_files": [
    "src/ingestion/handler.py",
    "src/ingestion/validator.py",
    "tests/test_ingestion.py",
    "docs/api-reference.md"
  ],
  "criterion_mapping": [
    {
      "criterion_id": "behavior-api-validates-records",
      "steps": [
        "Extract record from request body",
        "Run JSON Schema validation",
        "Forward valid records to the processing pipeline unchanged"
      ]
    },
    {
      "criterion_id": "documentation-api-reference",
      "steps": [
        "Describe accepted records and validation failures in the API reference"
      ]
    },
    {
      "criterion_id": "code-quality-single-validation-path",
      "steps": [
        "Route the endpoint through the shared validator path"
      ]
    }
  ]
}

name = "bad-registry-reference"
purpose = "Demonstrate registry validation."
session_role = "test"
preconditions = ["input exists"]
start_node = "inspect"
failure_modes = ["registry reference is missing"]
corruption_modes = ["reference drift"]

[[nodes]]
name = "inspect"
intent = "Inspect input."
disciplines = ["orient", "missing-discipline"]
mechanics = ["read-artifact"]
outcomes = ["input inspected"]

[[edges]]
from = "inspect"
to = "done"
condition = { type = "always" }

[[terminals]]
name = "done"
outcome = "Done."
artifact_produced = "completion-evidence"

name = "submit-smoke"
purpose = "Exercise workflow mechanic registry resolution with a mechanic parser output."
session_role = "integration smoke"
preconditions = ["a mechanic parser registry exists"]
start_node = "produce-artifact"
failure_modes = ["mechanic reference drifts"]
corruption_modes = ["cross-parser drift"]

[[nodes]]
name = "produce-artifact"
intent = "Produce an artifact through a generic runtime mechanic."
disciplines = ["orient"]
mechanics = ["produce-artifact"]
outcomes = ["artifact produced"]

[[edges]]
from = "produce-artifact"
to = "delivered"
condition = { type = "always" }

[[terminals]]
name = "delivered"
outcome = "The artifact exists."
artifact_produced = "completion-evidence"
